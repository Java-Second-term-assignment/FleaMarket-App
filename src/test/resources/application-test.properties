# 統合テスト用プロファイル
# 外部サービスは @MockBean で差し替えるため、実接続は不要

# メール送信を抑止（テストでは送信しない）
spring.mail.host=localhost
spring.mail.test-connection=false

# JWT はデフォルトの secret で問題なし（テスト専用）
# AWS / Stripe はテストクラスで @MockBean によりモック化
