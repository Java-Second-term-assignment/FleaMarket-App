package com.example.flea_market_app.admin.service;

import org.springframework.stereotype.Service;

/**
 * CSV出力（参照専用）。
 *
 * 原則:
 * - 書き込み禁止
 * - 大量データはストリーミング or 非同期ジョブが理想
 */
@Service
public class CsvExportService {
	// TODO: implement later
}
